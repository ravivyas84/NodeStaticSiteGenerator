extends ./base.pug

block head 
    meta(rel= 'canonical', href= `${options.canonical}`)

block content
    .container.content 
        h1.title #{options.heading}
        div.postDate #{options.humandate}
    .container.content !{options.body}
